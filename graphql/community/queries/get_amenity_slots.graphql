query GetAmenitySlots($amenityId: String!, $startDate: DateTime!, $endDate: DateTime!) {
  amenity(id: $amenityId) {
    id
    name
    slots(startDate: $startDate, endDate: $endDate) {
      id
      date
      startTime
      endTime
      status
    }
  }
}
